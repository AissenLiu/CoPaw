[package]
name = "copaw-desktop"
version = "0.1.0"
description = "Tauri desktop shell for CoPaw"
authors = ["CoPaw Team"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.0.0" }

[dependencies]
serde = { version = "1.0", features = ["derive"] }
tauri = { version = "2.0.0" }
